{
    "prefix": [
        [{
            "command": "prefix",
            "waitCommand": "prefix_wait"
        }],
        [{
            "command": "prefix2",
            "waitCommand": "prefix2_wait"
        }]
    ],
    "suffix": [
        [{
            "command": "suffix",
            "waitCommand": "suffix_wait"
        }],
        [{
            "command": "suffix2",
            "waitCommand": "suffix2_wait"
        }]
    ],
    "deploy": [
        [{
            "command": "deploy",
            "waitCommand": "deploy_wait"
        }],
        [{
            "command": "deploy2",
            "waitCommand": "deploy2_wait"
        }]
    ],
    "delete": [
        [{
            "command": "delete",
            "waitCommand": "delete_wait"
        }],
        [{
            "command": "delete2",
            "waitCommand": "delete2_wait"
        }]
    ],
    "validate": [ "validate" ]
}